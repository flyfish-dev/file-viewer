name: Issue 235 workspace bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the next bootstrap commit to validate Issue 235."
