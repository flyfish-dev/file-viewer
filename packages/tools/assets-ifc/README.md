# @file-viewer/assets-ifc

显式 `@file-viewer/capability-ifc` 的自托管 runtime 资产包。所有 IFC 都使用 That Open Components + Fragments；`web-ifc@0.0.77` 作为 `IfcLoader` 底层解析器。资产包包含 `web-ifc.wasm`、`web-ifc-mt.wasm`、Fragments worker，以及 MPL-2.0 / MIT 许可证文本，不使用公共 CDN。
