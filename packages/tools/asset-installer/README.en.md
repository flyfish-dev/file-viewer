# @file-viewer/asset-installer

Public internal helper for independently versioned File Viewer capability asset packs. It performs contained, transactional merges and writes one SHA-256 receipt per asset package. It does not contain renderer assets.
