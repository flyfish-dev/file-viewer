# @file-viewer/assets-cad

Self-hosted CAD Worker and WebAssembly assets for the opt-in CAD renderer. Install them explicitly with `@file-viewer/cli` or run `file-viewer-assets-cad`; they are not included in the standard full packages.
