# @file-viewer/capability-text-tools

Registers the optional runtimes used for side-by-side patch comparison and Git bundle decompression. Basic text, code, and Markdown rendering does not install them.
