# Third-party notices

- `@ssabrojs/hwpxjs` 0.4.0 is used under the MIT license for structured HWP 5.x and HWPX parsing. Its HWP path is documented upstream as a TypeScript port of the MIT-licensed `rhwp` parser. The renderer preserves explicit errors for encrypted and distribution documents.
- `cfb`, `JSZip`, `pako`, and `@xmldom/xmldom` are used in the on-demand Hangul Worker for bounded container, stream, and XML processing.
