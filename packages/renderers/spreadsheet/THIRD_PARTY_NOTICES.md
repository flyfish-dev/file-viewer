# Third-party notices

- `styled-exceljs` supplies Apache-2.0 licensed XLS/XLSX workbook, style, drawing, and chart parsing.
- `UTIF.js` is loaded only when an embedded TIFF drawing needs browser conversion. It is MIT licensed; File Viewer bounds encoded bytes, dimensions, and decoded pixels before conversion.
- `e-virt-table`, `JSZip`, `@xmldom/xmldom`, and `tinycolor2` remain scoped to the on-demand spreadsheet renderer.
