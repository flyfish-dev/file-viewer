# v2.3.0 issue reproduction and release plan

Last audited: 2026-08-22 (Asia/Shanghai)

This ledger is the release source of truth for every open issue in
`flyfish-dev/file-viewer`. An issue is closed only after its original attachment, or a
documented equivalent fixture, passes the owning project's automated test and a real browser
check. A screenshot without the source document is not sufficient evidence for closure.

## Current inventory

| Issue | Target | Reproduction evidence | Required result | Status |
| --- | --- | --- | --- | --- |
| [#174](https://github.com/flyfish-dev/file-viewer/issues/174) Keynote | `packages/renderers/iwork` in File Viewer | The current tree contains Keynote IWA/iWork 09/current fixtures, parser goldens, and browser coverage. The original request has no attachment. | Re-run the Keynote fixture and browser gates from the release candidate. Close only after the published v2.3.0 package and Demo expose the tested route. | Released in v2.3.0; local parser, pixel, and three-generation browser verification passed, the Demo route is live, and the issue is closed |
| [#178](https://github.com/flyfish-dev/file-viewer/issues/178) Excel embedded images | Spreadsheet renderer plus `styled-exceljs` | Generated fixtures cover Office 365 rich-data images, WPS `DISPIMG`, floating XLSX drawings, legacy XLS OfficeArt, GLB fallback, Worker transport, and double-click preview. The reporter's follow-up workbook embeds an uncompressed 623×148 RGB TIFF drawing, which reproduced as `application/octet-stream` and a browser-undecodable `<img>`. | Preserve TIFF MIME metadata in `styled-exceljs`, lazily decode bounded TIFF data in File Viewer, and verify the exact attachment in main-thread, Worker, and double-click preview paths without regressing column resizing. | Released with `styled-exceljs@0.21.2`, `@file-viewer/renderer-spreadsheet@2.3.2`, and the Office/Full 2.3.3 line. The exact attachment passed main-thread, Worker, double-click lightbox, npm cold-install, and production Demo checks; production column resizing moved 82 px to 130 px. Issue closed after the verified correction. |
| [#179](https://github.com/flyfish-dev/file-viewer/issues/179) PPT runtime dynamic import | Demo/runtime wiring in File Viewer | v2.2.5 configured `vendor/ppt/index.mjs` as a native dynamic-import override. Static servers that return a non-JavaScript MIME type reproduce `Failed to fetch dynamically imported module`. Vite development also reproduced a MIME failure when dependency prebundling expanded the package's dynamic font URL into a broad asset glob. | Bundle the public ESM module and keep only Worker/WASM/font files as URL assets. Apply the asset rewrite in build and development, exclude the package from dependency prebundling, and verify both modes in a browser. | Released in v2.3.0; production and development Vite browser verification passed, and the issue is closed |
| [#195](https://github.com/flyfish-dev/file-viewer/issues/195) GBK text | Text renderer in File Viewer; spreadsheet text import is a separate path | The public `1.txt` attachment is four bytes (`b2 e2 ca d4`, SHA-256 `5111b58e…`) and decodes to `测试` as GBK. The spreadsheet CSV/TSV path already detects GBK, but a plain `.txt` must still be tested through its own renderer. | Preserve UTF-8/UTF-16 handling, add GBK/GB18030 detection to normal and virtual text paths, and verify the exact attachment in a browser. | Released in v2.3.0; the exact attachment passed unit and browser verification, and the issue is closed |
| [#198](https://github.com/flyfish-dev/file-viewer/issues/198) CAD Worker path | Vite asset copying in File Viewer; the CAD renderer/runtime already ships the required source files | The Full/CLI paths contain both `wasm/cad/dwg-worker.js` and `wasm/cad/0.8.0/dwg-worker.js`, but a registry-cold standard Vite project using `@file-viewer/vite-plugin@2.3.0` reproduces the user's follow-up exactly: `copyAssets:true` reports success while emitting only the flat `wasm/cad/*` files. | The Vite plugin must preserve flat compatibility aliases and mirror the installed CAD runtime into its validated version directory. Re-run the exact registry-cold plugin path, request the versioned worker URL, and render a real DWG before closing again. | Fixed and published as `@file-viewer/vite-plugin@2.3.1`. A post-publish registry-cold Vite build emitted both paths; Chrome loaded the exact `0.8.0` Worker, LibreDWG JS/WASM, and Autodesk DWG with 16 layers and a 1028×852 WebGL canvas. Issue closed after the verified correction. |
| [#200](https://github.com/flyfish-dev/file-viewer/issues/200) PPTX layout | Normally `@file-viewer/pptx`; ownership cannot be finalized without the PPTX | The report contains two screenshots but no source presentation. The screenshots alone cannot reproduce the responsible XML, fonts, SmartArt, table, or text-box path. | Keep open and request the original PPTX. A separately constructed fixture may improve a known parser path but cannot prove this report fixed. | Not reproducible; retain open |
| [#201](https://github.com/flyfish-dev/file-viewer/issues/201) German locale | Core locale contract plus Demo/ecosystem components in File Viewer | Locale inventory currently exposes Chinese, English, and Japanese; `de-DE` is absent. The issue includes the requested translation catalog. | Add the complete `de-DE` catalog, locale normalization/selection, type coverage, and browser/API regressions without changing existing locale fallbacks. | Released in v2.3.0; the 449-key contract, type checks, and real `de-DE` browser verification passed, and the issue is closed |
| [#202](https://github.com/flyfish-dev/file-viewer/issues/202) complex DOCX layout | `@file-viewer/docx` in `/Users/wangyu/WebProjects/office/docxjs`, then File Viewer integration | The public DOCX (SHA-256 `a09d1df0…`) contains 20 sections, 241 frames, 18 text boxes, 38 drawings and 4 tables. The current browser renderer creates 19 articles with heights from 1 px to 3133 px, proving collapsed positioned/page content. | Fix the page/frame/section behavior in the DOCX engine, publish an exact dependency version, integrate it here, and compare the original 20-page attachment in a browser. | Released in v2.3.0 with `@file-viewer/docx@0.3.27`; dependency PRs #3 and #4, 26 engine regressions, the original-file browser integration, and cold install passed; the issue is closed |
| [#203](https://github.com/flyfish-dev/file-viewer/issues/203) Excel charts and widths | Spreadsheet renderer and maintained `styled-exceljs` | Both public XLSX files were parsed. Their chart XML uses formulas without usable cached points, so the current chart parser returns no definitions. The supply-chain workbook also makes the current MDW inference render `明细台账` at only 326 px including row headers, while the same workbook's sheets receive incompatible width scales. A final browser audit additionally reproduced enabled column resizing failing inside Shadow DOM because retargeted global mouse events no longer passed e-virt-table's containment check. | Resolve chart series/title/category formulas from workbook cells when caches are absent. Make OOXML column-width conversion stable for one workbook and across sequential opens. Preserve interactive column resizing in Shadow DOM and verify both original files plus a real header drag in a browser. | Released in v2.3.0; both exact workbooks passed parser and browser chart/width checks, the production Demo drag moved 54 px to 102 px, and the issue is closed |
| [#204](https://github.com/flyfish-dev/file-viewer/issues/204) Excel column resize regression | Spreadsheet renderer in File Viewer | The report enables `spreadsheet.resizableColumns` and observes that row resizing works while column resizing does not, including in the official Demo. This matches the independently reproduced Shadow DOM event-retargeting failure covered by the #203 browser audit. | Keep the option enabled across standard and full component packages, publish the corrected spreadsheet dependency chain, and verify a real header drag in the production Demo. | Published in `@file-viewer/renderer-spreadsheet@2.3.1` and Office/Full 2.3.2; production Demo verification moved 54 px to 102 px, and the issue is closed |
| [#205](https://github.com/flyfish-dev/file-viewer/issues/205) PPTX-only Vite output | `@file-viewer/renderer-presentation` plus `@file-viewer/vite-plugin`; this is not a core defect | A production Vite build from the v2.3.0 baseline with only `formats: ['pptx']` emitted the 16,437,364-byte classic-PPT CJK font and the 1,557,729-byte classic-PPT WASM. The generic presentation entrypoint necessarily exposes both lazy engines, so the format mapping could not remove the binary route. | Publish stable `/ppt` and `/pptx` renderer subpaths, map explicit formats and renderer IDs to the matching subpath, and prove a real PPTX worker remains while classic-PPT filenames and content references are absent. Preserve the aggregate root entrypoint for presets and applications that support both families. | Released through PR #207 as `@file-viewer/renderer-presentation@2.3.1` and `@file-viewer/vite-plugin@2.3.2`. All 57 registry tarballs passed verification; a registry-cold Vite 6 build emitted the PPTX Worker without classic-PPT filenames, CJK font, WASM, or content references. Issue closed after the verified correction. |
| [#206](https://github.com/flyfish-dev/file-viewer/issues/206) cryptographic containers | New optional File Viewer renderer packages, not core. CMS/timestamps and ASiC may share bounded parsing and nested-preview contracts; OpenPGP must remain a separately gated capability. | The request spans CMS/CAdES, RFC 3161/5544, ASiC, evidence records, OpenPGP, PAdES, XMLDSig/XAdES, JWS/JAdES, and S/MIME, but provides no non-secret fixtures. A separate rPGP 0.20.0 feasibility build confirmed `wasm32-unknown-unknown` support and MIT/Apache-2.0 licensing. A binary-message parser measured 808,008 bytes raw / 181,283 bytes Brotli; armor plus message, signature, public-key, private-key, and cleartext classification measured 1,456,400 bytes raw / 328,805 bytes Brotli (1,258,933 / 325,902 after `wasm-opt -Oz`). | Split delivery by standards family, require immutable public or generated fixtures for every claimed route, keep all parsing offline and bounded, and distinguish parsing, digest matching, cryptographic verification, trust, policy, and legal validity. Do not import, bundle, copy, or derive from LGPL OpenPGP source. rPGP is viable only as an optional lazy Worker/WASM path: upstream describes it as low-level building blocks rather than certificate/key semantics and records the RustCrypto RSA side-channel limitation. | Roadmap epic; the permissive rPGP path is feasible but not a lightweight core dependency and does not implement the wider request. Keep open pending fixtures and a license-clean first phase. |

GitHub had nine open issues after #204 was filed. After the v2.3.0 production verification,
eight reproducible issues were closed. The current open inventory is #200 (missing source PPTX)
and #206 (fixture-free multi-standard feature epic). #205 is closed after its immutable npm corrections
passed registry-cold verification.
The closed v2.3.0 milestone also includes #178 because this is its first complete
cross-channel release. #196 is intentionally excluded from release milestones because it belongs
to a different project and was closed as not planned. Gitee returned no open issues. GitHub Dependabot and secret-scanning both returned zero
open alerts; code scanning has no analysis configured, which is recorded as unavailable rather
than reported as a passing scan.

## Closed-issue follow-up audit

Closed issues were also checked for comments posted after the maintainer's last resolution. They
are not reopened merely because they are old; the original evidence must be re-run first.

| Issue | Follow-up finding | Current evidence | Disposition |
| --- | --- | --- | --- |
| [#36](https://github.com/flyfish-dev/file-viewer/issues/36) DOCX image stretching | The reporter's original `demo1-docx.docx` received a post-close screenshot showing a vertically stretched Office 365 SVG picture. This belongs to the self-maintained `@file-viewer/docx` engine first, then File Viewer integration. | The original attachment has SHA-256 `4a5e3979e40d3560dc8c00b0be21340f3f3645e6d311813b57815215d5127354`. Its OOXML extent is 203.42×50.86 pt and the SVG intrinsic size is 118×30. Current `@file-viewer/docx@0.3.27` plus the live component path renders 271.22×67.81 CSS px with `object-fit: contain`, 9 images, 3 sections, and no browser errors. The regression rejects any substitute fixture by SHA-256. | Fixed by the current dependency/integration; keep closed and reply once with the exact-file result. |
| [#80](https://github.com/flyfish-dev/file-viewer/issues/80) EPUB JSZip path | Vite 6 previously rewrote `jszip` to `jszip/lib/index.js/dist/jszip`. | The registry-style React/Vite 6.3.6 build completes and emits the EPUB renderer chunk without the broken path. The exact subpath now has an exact alias to `jszip/dist/jszip.js`. | Still fixed; no new reply because the issue already has a maintainer resolution and no later feedback. |
| [#110](https://github.com/flyfish-dev/file-viewer/issues/110) Vite 8 xmldom interop | A spreadsheet route previously loaded the CommonJS `@xmldom/xmldom` entry directly and could not find the `DOMParser` named export. | The current Vite plugin keeps `@xmldom/xmldom` in CJS optimization, the Vite 8 auto-scan/asset suite passes, and a real Excel browser route renders without module errors while preserving column drag from 54 px to 102 px. The reporter had already confirmed the prior release. | Still fixed; no repeated reply. |
| [#156](https://github.com/flyfish-dev/file-viewer/issues/156) deployed PDF Worker | The screenshot shows `/file-viewer/vendor/pdf/pdf.worker.mjs` failing and PDF.js reporting `Setting up fake worker failed`. The reporter later said they found the cause, but another user asked for the resolution. | A real-browser harness forces that exact Worker path to HTTP 404. The current renderer probes the unusable asset, selects its packaged same-version handler, renders two PDF pages/canvases, and does not emit the reported fake-worker error. `fileViewerRenderers({ copyAssets:true })` remains the preferred real-Worker deployment path. | Keep closed and post one concise resolution for the unanswered follow-up. |

The same audit caught a release-harness defect not tied to a public issue: when the workspace Demo
served `@file-viewer/pptx` directly from `/@fs/.../src/worker.ts`, the default URL pointed at the
non-existent `src/worker/pptx.worker.js`. Source-linked Vite now resolves the sibling package's
`dist/worker/pptx.worker.js`, while the static production URL expression is preserved. Angular 22,
PPTX-only bundling, a real PPTX browser render, and the DOCX/OFD/PPTX print-mask adapters all pass.

## Execution order

1. Add immutable issue fixtures (original URL, SHA-256, expected behavior) and failing unit/browser regressions.
2. Fix self-developed engines in their owning repositories first, publish exact versions, then update File Viewer. Do not hide engine defects with outer-viewer coordinate or filename patches.
3. Fix File Viewer-owned parsing, localization, asset, and Demo wiring defects.
4. Run targeted tests, type checks, package builds, real browser checks, and cold-install asset checks for every issue.
5. Reply briefly in English with `gh` only. Close only the issues whose original or equivalent fixture passed; leave non-reproducible reports open with the missing evidence stated.
6. If the repository and release-channel gates are clean, commit and push the private source, generate and push the GitHub public boundary, publish the full v2.3.0 npm ecosystem and GitHub Release, deploy Demo/docs/site/component demos and Docker, then run production-domain and strict post-release checks.

## Release stop conditions

The v2.3.0 release must stop rather than be claimed complete if npm authentication/MFA,
GitHub Release assets, Cloudflare production checks, Docker multi-architecture publication, or
the strict channel postcheck cannot be completed. Optional Gitee mirror limitations are recorded
but do not block the core GitHub/npm/Docker/Cloudflare release.
